version: '3.8'

services:
  servidor:
    build: .
    command: ./servidor
    ports:
      - "65432:65432"
    volumes:
      - ./dados:/app/dados
      
  cliente:
    build: .
    command: ./cliente
    depends_on:
      - servidor